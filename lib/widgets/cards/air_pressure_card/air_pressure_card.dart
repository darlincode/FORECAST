import 'package:flutter/material.dart';

class AirPressureCard extends StatelessWidget {
  const AirPressureCard({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
