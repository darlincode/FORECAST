import 'package:flutter/material.dart';

class AQICard extends StatelessWidget {
  const AQICard({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
