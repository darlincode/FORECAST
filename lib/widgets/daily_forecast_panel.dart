import 'package:flutter/material.dart';

class DailyForecastPanel extends StatelessWidget {
  const DailyForecastPanel({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
