import 'package:flutter/material.dart';

class HourlyForecastPanel extends StatelessWidget {
  const HourlyForecastPanel({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
